# AGENTS.md — AI 协作规则

本仓库对 AI 助手的硬性规则。违反任何一条视为无效操作。

## 规则一：不自行提交、不自行推送

- 任何改动完成后不得自行 `git commit` / `git push`。
- 只有用户在对话中**明确要求提交**时才 commit；只有用户**明确要求推送**时才 push。
- 「提交」不隐含「推送」，「推送」不隐含「提交」，逐级都需要用户明确说出。说「提交」就只 commit，绝不顺带 push。
- 用户更倾向自己执行提交（更快），因此**默认产出可直接复制的提交命令与 commit message 交给用户执行**，而不是自己动手；用户明确说「你来提交」时才代为执行。
- 正确流程：改完 → 自查（diff / 测试）→ 报告（改动内容 + 建议 commit message）→ 等用户指示。

## 规则二：改动前检查工作区

- 动手修改任何文件之前，先运行 `git status` 检查仓库是否有未提交变更。
- 如果存在未提交变更（含未跟踪文件），必须先向用户报告并确认，得到许可后才能开始修改。

## 规则三：禁止内嵌仓库（gitlink）

新增或从别处移入技能目录时，**目录内不得包含 `.git`**（目录或文件）。否则 `git add` 会把它当作内嵌仓库，以 gitlink（mode 160000）写入索引——GitHub 上该目录会变成不可点开的灰条目，且引用了一个本仓库并不存在的 commit，后续操作与推送都会出问题。

- 移入前先执行：`find <新目录> -name .git`，有结果就先删掉 `.git` 再纳入本仓库。
- 提交前自检，输出必须为空：`git ls-files -s | awk '$1=="160000"'`。
- 技能目录不得自带 `.gitignore`、不得留空的 `assets/` `scripts/` 骨架目录——忽略规则统一由仓库根 `.gitignore` 维护。
- 补救：若已误提交 gitlink，用 `git rm --cached <目录>` 后重新以普通文件 `git add`，不要用 `git submodule`。
